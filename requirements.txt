fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0

pandas>=2.0
openpyxl>=3.1
python-calamine>=0.2   # optional but recommended: fast Excel engine



langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-core>=0.3,<0.4        # only for the Embeddings base class
huggingface_hub>=0.24,<2.0      # embeddings via HF's hosted Inference API - see
                                 # rag_engine.get_embeddings() / _HFInferenceEmbeddings
                                 # (deliberately NOT langchain-huggingface - its
                                 # HuggingFaceEndpointEmbeddings calls a method
                                 # huggingface_hub has since removed)
langchain-groq>=0.2,<0.3
chromadb>=0.5

playwright>=1.44   # headless-browser URL fetch, see rag_engine.py load_url()
                    # after installing: run `playwright install chromium` once

pypdf>=4.0            # PyPDFLoader
docx2txt>=0.8         # Docx2txtLoader
unstructured>=0.14    # UnstructuredHTMLLoader
beautifulsoup4>=4.12  # BSHTMLLoader fallback + WebBaseLoader parsing
lxml>=5.0
requests>=2.31